# ── Core Framework ──────────────────────────────
fastapi
uvicorn[standard]
gunicorn
pydantic
pydantic-settings

# ── Authentication & Security ───────────────────
python-jose[cryptography]
passlib[argon2]
argon2-cffi
python-multipart

# ── LLM & API Clients ──────────────────────────
groq
openai
anthropic

# ── Agent Orchestration ─────────────────────────
langgraph
langchain-core
# langchain-ollama is kept for local use but won't be called in cloud

# ── Agent Sandbox (Code Execution) ──────────────
RestrictedPython

# ── Database & Caching ──────────────────────────
redis
sqlalchemy
aiosqlite

# ── HTTP & Async ────────────────────────────────
httpx
aiohttp

# ── Utilities ───────────────────────────────────
python-dotenv

# ── Note on ML ──────────────────────────────────
# torch, transformers, and scikit-learn are removed for Cloud Free Tier 
# compatibility (512MB RAM). Security classifiers will run in passthrough mode.